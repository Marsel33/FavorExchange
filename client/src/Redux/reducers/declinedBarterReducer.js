import initState from "../init/initState";

export const declinedBarterReducer = (state = initState, action) => {
    switch (action.type) {
        
        default :
            return state
    }
}