import initState from "../init/initState";

export const endedBarterReducer = (state = initState, action) => {
    switch (action.type) {
        case:
        default:
            return state
    }
}